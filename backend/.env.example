# PostgreSQL connection (use a dedicated database per environment)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/snf"

# Google API keys (optional; without them, geocoding/place lookups fall back gracefully)
GOOGLE_MAPS_KEY=""
GOOGLE_PLACES_API_KEY=""
GOOGLE_PLACE_IDS_SEED_PATH="" # optional CSV seed for place_ids; leave empty to disable

# Cache freshness controls
GOOGLE_REVIEWS_MAX_AGE_DAYS=7
GOOGLE_PLACE_ID_MAX_AGE_DAYS=30

# Misc
CMS_PROVIDER_BASE_URL=https://data.cms.gov/provider-data/api/1/datastore/query
CMS_PROVIDER_HOSPITAL_DATASET=xubh-q36u
CMS_PROVIDER_SNF_DATASET=4pq5-n9py
CMS_FORCE_REFRESH=0
USE_LOCAL_HOSPITAL_FALLBACK=1
USE_LOCAL_SNF_FALLBACK=1
NOMINATIM_USER_AGENT="SNF-Referrals/1.0 (+https://example.com/contact)"
PORT=8080
